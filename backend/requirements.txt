# ── Core ─────────────────────────────────────────────────────────────────────
pydantic>=2.9,<3.0
pyyaml==6.0.2
python-dotenv==1.0.1
jinja2==3.1.4
orjson>=3.10,<4.0

# ── Data handling ────────────────────────────────────────────────────────────
pyarrow==18.1.0
pandas==2.2.3
polars>=1.18,<2.0
duckdb>=1.1,<2.0
datasets==3.2.0

# ── Text matching / normalization ────────────────────────────────────────────
rapidfuzz>=3.10,<4.0
pyvi==0.1.1
underthesea==6.8.4

# ── Embeddings / Reranking / LLM local ───────────────────────────────────────
sentence-transformers==3.3.1
transformers==4.47.3
accelerate==1.2.1
einops==0.8.0
huggingface-hub==0.27.0

# Nếu chạy trong NVIDIA/DGX container đã có torch tối ưu sẵn,
# có thể bỏ dòng torch này và dùng torch có sẵn trong image.
torch==2.5.1

# ── Vector DB ────────────────────────────────────────────────────────────────
qdrant-client>=1.12,<2.0

# ── Lexical search ───────────────────────────────────────────────────────────
opensearch-py>=2.7,<3.0

# ── Graph DB ─────────────────────────────────────────────────────────────────
neo4j==5.27.0

# ── LLM API client: local vLLM OpenAI-compatible ─────────────────────────────
openai==1.59.9

# ── Logging / progress ───────────────────────────────────────────────────────
tqdm>=4.66,<5.0
loguru>=0.7,<1.0
rich>=13.9,<14.0

# ── Testing / code quality ───────────────────────────────────────────────────
pytest==8.3.4
pytest-mock==3.14.0
ruff>=0.8,<1.0